services:
  bot:
    build: .
    container_name: combustion-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data.json:/app/data.json
      - ./config.json:/app/config.json
